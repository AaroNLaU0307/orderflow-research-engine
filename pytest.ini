[pytest]
markers =
    data: test touches the real bar/bucket store or another real downloaded file (data/parquet, data/raw, etc.), not a synthetic fixture - excluded from CI via `pytest -m "not data"` since CI does not download the ~54GB dataset.
